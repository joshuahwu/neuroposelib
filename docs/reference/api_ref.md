# API Reference — neuroposelib

This page provides a short overview and quickstart for the `neuroposelib` package.
For the full API documentation see the per-module pages linked in the sidebar.

**Quick links**

- [`DataStruct`](neuroposelib.DataStruct.DataStruct) — data container used across the package
- [`Embed`](neuroposelib.embed.Embed) — create and apply embeddings
- [`GaussDensity`](neurposelib.embed.GaussDensity) / [`Watershed`](neuroposelib.embed.Watershed) — density maps and density-based clustering